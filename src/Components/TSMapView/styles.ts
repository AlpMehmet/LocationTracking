import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  map: {
    flex: 1,
  },
  markerIcon: {
    borderWidth: 1,
    borderColor: '#000000',
    backgroundColor: 'rgba(0,122,111, 0.5)',
    width: 12,
    height: 12,
    borderRadius: 5,
  },
});

export default styles;
